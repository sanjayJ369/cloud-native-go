syntax = "proto3";

package store;

option go_package = "./proto/store";

message GetRequest {
	string key = 1;
}

message GetResponse {
	string value = 1;
}

service StoreService {
	rpc GetHandler(GetRequest) returns (GetResponse);
}
